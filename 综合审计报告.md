# DeepSharkViewStudio 综合工程审计报告

> 审计日期：2026-07-10  
> 审计对象：`DeepSharkViewStudio` 当前工作区，Git `main` / `bc2e938`  
> 审计方式：第一阶段只读审计；以当前代码、配置、测试、文档和现有本地工件为准  
> 最终定级：**Developer Build**；尚未达到 **Team Beta**，更不是 **Demo Release Candidate**

## 1. 执行摘要

本轮没有发现已经证实的 P0（立即、不可恢复的数据破坏或安全事故），但存在多项足以阻断团队分发和教师演示的 P1。

最重要的结论如下：

1. 四条目标链路的代码主体都存在，但 UI 中还有一条额外的 **B-2 Candidate View** 前置分流。当前仓库只要发现最新 B-2 candidate，就会默认把 Live Far-field 送入实验性 B-2，而不是名义上的 Far-field Default。当前运行日志已实际记录 `stitch_mode=candidate, runtime_mode=far_field`，这不是纯静态推断。
2. **Far-field Custom 的新建/预期路径仍是 B-2 per-camera projection + post-warp camera adjust + B-2 weight selection**；但 runtime 仍接受 legacy `current_perspective` candidate，并且实际 compositor 不读取 candidate 声明的 `far_field_blend.mode`，所以不能笼统声称“所有 Far-field Custom 都必然是 B-2”。当前工作区也没有任何已保存的 Far-field Layout Candidate，无法直接演示该链。
3. Near-field Current Perspective 与 Near-field Fisheye Rectilinear 都通过 `ProjectionProvider` 输出独立三路 warped image/mask，再进入同一 front-priority compositor。Fisheye 是 rectilinear remap 后复用 template homography，不是 equirectangular，也不是完整物理全景模型。
4. UI 不是单一状态机，而是同时维护显示布局、内容状态、runtime mode、candidate/template strategy、worker processor mode 和 QGC sink 状态。Load/Clear candidate、保存相机配置、导入项目、切语言等路径会出现“UI 已更新、后台仍运行旧对象”的确定性错位。
5. 单路断流后，GUI 的持久 `self.frames` 不会剔除失败相机的旧帧；只要其他相机继续更新，旧帧仍可能参与拼接并送往 QGC。全部 worker 结束时，页面仍可保持 Live 状态。
6. FFmpeg stdin 在 GUI 主线程同步 `write+flush`，stderr 使用无人消费的 `PIPE`；网络、编码器或 stderr 背压都可能冻结 UI。capture/stitch 线程为 non-daemon，关闭超时结果又被忽略，存在窗口关闭后进程不退出的风险。
7. 项目包目录复制后，相对路径资源能够继续手工加载；但 activation 只复制三个 config，完全不读取 manifest 的 `runtime_defaults`，不会恢复 Far/Near mode、candidate 和 Fisheye source。故“文件级可搬动”成立，“一键恢复同一运行态”不成立。
8. package validator 存在严重假阳性：损坏 config YAML、假的 B-2 NPZ、错误 candidate format 都可能校验通过；candidate 内的 `../` remap 路径还可能让 exporter 在 package root 外写文件。
9. 当前本机合成帧微基准与 `process_fps: 30` 明显不匹配。CPU 路径约为：Far Default 4.6–7.6 FPS、Near Current 2.6–3.5 FPS、Near Fisheye 约 2.6 FPS、Far Custom 约 6.5 FPS、B-2 direct 约 15–16 FPS；这些均尚未计入真实 capture、Qt 显示、FFmpeg/H.264 与 QGC。
10. 194 个 unittest 方法均已分批执行：191 通过、3 失败。两项 topology 失败证明 `dual_horizontal` 当前配置确实不可运行；另一项仍期待 feather=160，而当前实现/配置已改为 400，是过时断言。另有一次 OpenCV/OpenCL 原生进程 `0xC0000005` 崩溃，复跑通过，属于环境稳定性风险。

综合判断：当前更像一个功能广、研究链路多、已有良好局部边界和较多单元测试的开发者工具，但还不具备 Team Beta 所需的可复制安装、确定性默认模式、事务化迁移、稳定生命周期与端到端验收；更不具备 Demo RC 所需的现成正式工件、可兑现帧率、实机断流韧性和 QGC 证据。

## 2. 审计边界、方法与限制

### 2.1 已做

- 检查了当前 Git 状态、提交历史、全部 tracked 源码/测试/文档和实际 ignored 配置/候选/日志工件。
- 对核心 runtime、projection、candidate、UI、capture worker、stitch worker、QGC/FFmpeg、project/package、配置持久化做了调用链追踪。
- 先阅读测试写入行为，再以 `PYTHONDONTWRITEBYTECODE=1`、临时目录和 offscreen GUI 方式运行定向测试；GUI 测试将应用 logger 替换为内存 no-op，避免改写真实日志。
- 对当前配置与真实 B-2 / Near candidate 做了只读内存加载、package 临时目录搬移验证和合成帧微基准。
- 测试后复核 `calibration.yaml`、`cameras.yaml`、`network.yaml` 哈希和 Git 状态；仓库保持 clean。

### 2.2 未做

- 未连接三台真实相机，未改变任何真实 RTSP、配置、candidate 或项目数据。
- 未启动真实 QGroundControl、MediaMTX 或长时 RTSP 推流。
- 未做 30 分钟以上 soak、断网/拔线、GPU driver 故障、系统休眠/恢复或多机部署。
- 未在 Linux/macOS 验证；当前 launcher 和 path resolver 明显以 Windows 为中心。
- 微基准使用合成 1920×1080 三路帧，只能说明本机计算预算，不等价于真实端到端 FPS、时延或画质。

## 3. 当前事实基线

| 项目 | 当前事实 | 证据/备注 |
|---|---|---|
| Git | `main` 与 `origin/main` 对齐，审计前后 clean | `git status --short --branch` |
| 代码规模 | `main_window.py` 8,324 行；核心 UI、校准、项目、QGC 仍集中于一个类 | `deep_shark_studio/gui/main_window.py` |
| 顶层 UI | Realtime Monitor、Layout Tuning、Calibration & Candidates、Projection Research、Project Management、Diagnostics & Logs | `main_window.py:2493-2516` |
| Projection Research | 只有说明文字，没有生产执行控件 | `main_window.py:2690-2710` |
| active topology | `triple_front_panorama`，三路，active canvas 2200×700 | `configs/calibration.yaml:6,75-105` |
| legacy root canvas | 2440×1800，不是当前 triple profile 的实际 runtime canvas | `configs/calibration.yaml:1-6` |
| 输入契约 | 1920×1080 raw frame pixels；迁移备注明确“not a final geometric calibration” | `configs/calibration.yaml:80-102` |
| 当前性能配置 | process 30 FPS、preview 30 FPS、max input width 960、live intrinsics off | `configs/cameras.yaml:41-46` |
| QGC 配置 | UDP MPEG-TS，15 FPS，1800×600，libx264 路径为 `ffmpeg` | `configs/cameras.yaml:47-55` |
| B-2 工件 | 2 个；最新输入 1920×1080、输出 1800×600 | `projects/calibration_candidates/.../candidate.yaml` |
| 最新 B-2 质量 | `experimental: true`、`report_only: true`、`recommended: false`、`apply_allowed: false`、`try_with_risk` | 最新 candidate `:21-31` |
| B-2 同步性 | software-near-time，非硬件同步 | 最新 candidate `:490-493` |
| Near candidates | 4 个，全部 schema V2 / Current Perspective；最新输出 1800×700 | `projects/layout_candidates/**/candidate.yaml` |
| Far candidates | 0 个 | 全工作区 `candidate_type: far_field_layout` 搜索 |
| 已导出 package | 0 个 `project.dcsvs.yaml` | 当前 `projects/` |
| 本地项目数据 | 约 2.3 GiB / 2,794 文件；calibration sessions 与 seam candidates 占比最高 | 只读目录统计 |
| Python 环境 | Python 3.10.11、OpenCV 4.13.0、NumPy 2.2.6、PyYAML 6.0.3、PySide6 6.11.1 | shared env |
| FFmpeg | PATH 可用，2025-08-20 build，包含 libx264 | `ffmpeg -version` |
| 构建元数据 | 无 pyproject、lockfile、CI、installer、license/changelog；requirements 仅 4 个最低版本 | 仓库根目录 |

## 4. 当前真实架构与模块边界

```text
app.py
  -> MainWindow (Qt 主线程 / UI 状态 / still 同步处理 / QGC GUI sink)
       -> CameraStreamManager
            -> 每相机一个 _CameraStreamWorker + cv2.VideoCapture
       -> StitchProcessingManager
            -> 一个 latest-only StitchProcessingWorker
                 -> CandidatePanoramaProcessor          [B-2 Candidate View]
                 或 RuntimeStitchController
                      -> SurroundStitcher                [Far Default]
                      -> ProjectionProvider + compositor [Far Custom / Near]
       -> FfmpegVideoSink -> FFmpeg -> UDP MPEG-TS / RTSP -> QGC

CalibrationSession -> B-2 candidate / reports / remaps
Layout Tuner       -> Far or Near layout candidate
ProjectPackage     -> configs + candidates + relative manifest
```

| 模块 | 当前职责 | 边界评价 |
|---|---|---|
| `config.py` | YAML load、单文件原子 replace、内容哈希 revision | 单文件能力较好；多文件事务和进程锁缺失 |
| `topology.py` | active profile 解析、seam 校验、horizontal feather | 清晰；但配置中 dual profile 已失效 |
| `stream_manager.py` | 每路 capture 线程、最新帧 snapshot、超时 stop | ownership 设计正确；无重连、frame-age 驱逐和强制退出 |
| `stitch_processing.py` | 单 worker、latest-only pending request | 能避免无界队列；effective mode 被上游双重分流破坏 |
| `stitch_runtime_controller.py` | Far/Near/Auto 分支、provider 选择 | 是合理核心边界；GUI Live 并非总会经过它 |
| `projection/runtime_providers.py` | Current、B-2、Fisheye provider | 统一 `ProjectionResult` 是正面设计；mask/cache/metadata 仍不严谨 |
| `seam/*compositor*` | Far custom weight selection、Near front priority | 算法边界可辨；候选 schema 与实际约束不完全一致 |
| `calibration_candidate.py` | B-2 求解工件、runtime remap/selection、OpenCL | 功能集中且较重；候选仍为 experimental/report-only |
| `project_package/*` | export、validate、activate、依赖复制 | 相对路径思想正确；validator 与 activation 安全性不足 |
| `qgc/*` | headless service、FFmpeg sink | 已超越旧 README 的 placeholder；尚无端到端运行保障 |
| `main_window.py` | 几乎全部 UI 和 orchestration | 8,324 行单类使状态边界、测试替身和生命周期难以维护 |

## 5. 四条核心链路与真实边界

### 5.1 Far-field Default

规范链路：

```text
RuntimeStitchController.process
  -> _process_far_field
  -> SurroundStitcher.process
  -> warp_all / warp
       -> resize to max_input_width
       -> optional pinhole undistort
       -> cv2.warpPerspective
  -> stitch
  -> compose_horizontal_feather
```

证据：

- 分支：`deep_shark_studio/stitch_runtime_controller.py:79-111`
- warp：`deep_shark_studio/stitcher.py:98-137`
- composition：`stitcher.py:139-167`、`topology.py:218-287`

边界：规范 Far Default 不走 `ProjectionProvider`，不走 Near front-priority，也不使用 B-2 remap。

但是，只有在 GUI 的额外 `live_stitch_mode == "template"` 时，Live 才真正走该链。当前工作区有 latest B-2 candidate，初始化会自动设为 `candidate`（`main_window.py:1671-1683`），worker 随后直接构造 `CandidatePanoramaProcessor`（`stitch_processing.py:84-102`）。Still preview 则始终同步新建 `RuntimeStitchController`（`main_window.py:2428-2438,5718-5730`）。因此当前同名 Far-field 在 Live 与 Still 中不等价。

### 5.2 Far-field Custom

新建/预期 B-2 链：

```text
Far Layout Candidate
  -> B2CandidateProjectionProvider
  -> CandidatePanoramaProcessor.warp_all
  -> 3 路独立 remap image + valid mask + B-2 forward weight
  -> post-warp x/y/scale affine（image、mask、weight 同步变换）
  -> hard argmax B-2 weight selection
  -> centered crop
```

证据：

- candidate loader：`seam/far_field_layout_candidate_runtime.py:48-151`
- provider：`stitch_runtime_controller.py:113-167`、`projection/runtime_providers.py:84-137`
- B-2 remap/weight：`calibration_candidate.py:1308-1446`
- camera adjust：`stitching/camera_layout_adjust.py:38-118`
- weight selection：`seam/far_field_custom_compositor.py:34-120,173-215`

结论：**当 candidate 的 `projection.source == b2_far_field_candidate` 时，Far Custom 确实仍基于 B-2 per-camera projection，并实际执行 `b2_weight_selection`。** 它不把 flattened B-2 final canvas 作为调参输入，也不进入 Near side suppression/front-preserve 分支。

限定条件：

- loader 仍接受 `projection.source=current_perspective`（`far_field_layout_candidate_runtime.py:79-86`）；此时控制器走 Current provider，compositor 回退 horizontal feather。
- loader 虽校验 `far_field_blend.mode` 字符串，runtime dataclass 不保存该值；compositor 实际按 provider metadata 决定分支（`far_field_custom_compositor.py:49-81`）。source/blend 可互相矛盾而仍被加载。
- GUI 默认 Far tuner 目标为 1800×700，但 B-2 canvas 只有 1800×600；crop 会静默夹到输入尺寸，saver 仍记录请求的 700。
- 当前没有现成 Far candidate，所以此链只有代码、单元测试和内存构造验证，没有可直接交给教师的工件。

### 5.3 Near-field Current Perspective

```text
Layout Candidate V2/V3
  -> CurrentPerspectiveProjectionProvider
  -> SurroundStitcher.warp_all
  -> warped_images + nonzero-derived valid_masks
  -> post-warp camera adjust
  -> front-priority compositor
  -> optional vertical safety
  -> crop
```

证据：

- candidate loader/V2 默认：`seam/layout_candidate_runtime.py:76-188`
- controller/provider：`stitch_runtime_controller.py:179-245`、`runtime_providers.py:40-78`
- Near compositor/core：`seam/near_field_compositor.py:21-64`、`seam/front_priority_compositor_core.py:48-118`

边界：Near 消费独立 warped images，不消费 Far 已经混合后的 canvas。它要求 `front_left/front/front_right` 三路同时存在。当前四个 Near candidate 都是 V2，因此当前已有 Near 工件都属于 Current Perspective。

当前风险是 valid mask 由像素是否非黑推导：真实黑色内容会被当作无效区，可能让侧相机覆盖 front 黑色目标。

### 5.4 Near-field Fisheye Rectilinear

```text
三路 opencv_fisheye intrinsics source
  -> build/reuse per-camera rectilinear map
  -> cv2.remap(raw fisheye frame)
  -> current template perspective homography
  -> geometry valid mask
  -> same Near front-priority compositor
```

证据：

- K/D loader：`projection/intrinsics_runtime_loader.py:41-131`
- map cache/per-frame remap：`projection/runtime_providers.py:181-317`
- template warp/mask：`runtime_providers.py:319-368`

边界：

- 必须三路都是 `model: opencv_fisheye`，输入尺寸必须精确匹配 intrinsics resolution。
- 不是 equirectangular，不是 B-2，也不影响 Far Default。
- remap 后仍复用在原 template/raw 视觉上调出的 homography，画质和对齐必须重新实机调参。
- Equirectangular runtime 仍明确被拒绝；B-2 工件内部的 `equirectangular_rotation_only` remap 是另一条预计算候选链，不能与 Near Fisheye 混为一谈。
- 当前无 V3 Near candidate；latest B-2 candidate 可被 loader 作为三路 fisheye intrinsics source，但它本身仍是 experimental、不可正式应用。

### 5.5 额外链：B-2 Candidate View

```text
latest_calibration_candidate
  -> CandidatePanoramaProcessor.process
  -> B-2 per-camera remap
  -> precomputed available-camera selection masks
  -> 1800×600 candidate canvas
```

该链绕过 `RuntimeStitchController`，却被 UI 默认叠在 Far-field 名下。它应被视为显式、独立、实验性的第五种 effective mode，而不应继续隐式截获 Far Default。

## 6. UI 信息架构与用户工作流

正面能力：

- 六个顶层 workspace 已比旧 README 的三 workspace 清晰。
- Preview layout（Grid/Focus/Stitched）与 stitch algorithm enum 在类型上分离。
- Calibration Wizard、candidate、diagnostics 和 Project Management 都有可见入口。
- 运行时状态摘要、candidate metadata、timing 和日志已有基础展示。

主要问题：

1. **Projection Research 是空壳页。** 真正的 projection 控件分散在 Realtime 与 Layout；研究 runner/report 没有生产入口。
2. **B-2 Candidate / Far Default / Far Custom 三种概念在 Realtime 同时叠加。** `Current Profile Template` 按钮甚至被隐藏（`main_window.py:2619`），用户很难保证回到 formal Default。
3. **候选选择是“pending 选择”还是“已经应用到 worker”没有区分。** Load/Clear 会立即刷新 UI/config，但 Live worker 只有点 Apply 才重配。
4. **保存相机源不会重启 capture。** UI/health 可显示新 URL，新 checkbox 也会改变 active set，但旧 worker 仍连接旧源。
5. **语言切换不是完整状态迁移。** 它停止 preview、重建整套 UI，却不停止 QGC sink；新 QGC 页面显示 stopped/Stop disabled，后台 sink 仍 active。大量运行期提示还直接硬编码中文，English 模式并不完整。
6. **导入项目时不先停止 Live/QGC。** 新配置/新 topology 与旧 capture/FFmpeg 可短暂或长期混用。
7. **`image_dir` 作为 per-camera source 在 stream manager 中只变成 Idle。** 无 worker 时 GUI 回退到全局 `input_dir`，忽略每路配置的 image_dir source。
8. **Projection/Far/Near tuner 共用缓存但缺少 provenance freshness。** target 或 projection 改变后仍可预览/保存旧基底。

## 7. 线程、状态机与生命周期

### 7.1 已有的正确设计

- `VideoCapture` 的 create/read/release 都由同一 camera worker 所有；测试验证 manager thread 不会跨线程 release。
- stitch worker 只保留一个 latest pending request，不会形成无界处理队列。
- session id/result id 能阻止大部分旧 session 结果覆盖新 session。
- RTSP 在支持时配置 open/read timeout；不支持时会明确记录 fallback。

### 7.2 关键风险

- `_CameraStreamWorker` 与 `StitchProcessingWorker` 都不是 daemon。fallback backend、USB/video file 或长 OpenCV 运算可以超过 2 秒；`MainWindow.closeEvent()` 忽略 stop/shutdown 返回值后继续关闭，Python 进程仍可能被活线程挂住。
- GUI `self.frames.update(latest_frames)` 从不删除失败/缺失 key。单路失败而其他路继续时，旧图无限参与后续拼接。
- all workers dead 时只停止 timer，不把 content mode 置为 Failed/Stopped，不停止 QGC，也不继续更新 frame age。
- 无自动 reconnect/backoff；首次 read failure 就退出 worker。
- candidate Load/Clear、camera Save、package Import/Reload 都没有对 capture、stitch worker、candidate object、Fisheye source 与 QGC sink 做原子状态切换。
- `reload_runtime_state()` 只尝试重载旧 Near candidate，不处理 Far/Fisheye，也不读取 package manifest；不同项目之间可残留旧对象。
- 相同 stitch error 每个 result id 都会再写日志；30 次提交/秒下可造成错误风暴和两个 QTextEdit 持续增长。

## 8. 性能与资源使用

### 8.1 当前配置与只读微基准

测试条件：本机 shared env、三路 1920×1080 合成帧、`max_input_width=960`、CPU/OpenCL 分开；不含真实 capture、Qt paint、FFmpeg 或 QGC。

| 模式 | 观察到的稳定耗时/理论结果 FPS | 备注 |
|---|---:|---|
| Far Default | 约 131–215 ms / 4.6–7.6 FPS | 2200×700；内容与运行轮次有差异 |
| B-2 Candidate CPU | 约 63–67 ms / 15–16 FPS | 1800×600 |
| Far Custom B-2 CPU | 约 155 ms / 6.5 FPS | 额外 affine + weight warp/stack/argmax |
| Near Current | 约 288–389 ms / 2.6–3.5 FPS | front-priority compositor 占主导 |
| Near Fisheye | 约 389 ms / 2.6 FPS | map cold start 另约 97 ms |
| B-2 Candidate OpenCL | 约 6.4 ms / 约 156 FPS 的内核级结果 | warped diagnostics 为空；不代表端到端能力 |

当前 `process_fps=30` 是提交节拍，不是可交付结果 FPS。latest-only worker 会不断替换 pending request，但 UI 没有 busy、submitted、dropped、result FPS 或 frame age 指标，教师会看到“30 FPS 设置”却得到 3–16 FPS 实际结果。

### 8.2 内存/带宽事实

- 三路 1920×1080 BGR 在 30 次 snapshot copy/秒时，仅 raw frame copy 理论量约 560 MB/s。
- latest B-2 processor 初始化约 0.34 秒；持久 NumPy arrays 约 64.9 MiB（maps、masks、weights、所有 camera-combination selection masks），不含输入帧、warped canvas、OpenCV/Qt 对象和每帧临时量。
- Fisheye map cache 约 53.4 MiB；metadata 每帧仍重复报告 cold build 常量。
- Far Custom 不使用 B-2 processor 预构建的 selection-mask combinations，却仍付出其构造/内存成本；每帧又重新 warp 三张 weight、stack 和 argmax。
- Near 的 `RuntimeNearFieldPlan` 是占位并被直接丢弃；identity camera adjustment 也会执行 image/mask affine。
- hidden root tab 下，只要 preview layout 仍为 Stitched，`should_render_canvas()` 仍返回 true；后台结果会继续做 BGR→RGB、QImage copy 和 SmoothScale。
- QGC resize 与 `frame.tobytes()` 每帧再分配；当前 1800×600×3、15 FPS 约 46 MB/s 原始 payload，若按 30 写入则约 97 MB/s，尚未计编码。

### 8.3 观测指标问题

- `far_field_custom_total_ms` 从 compositor 内开始计时，不含前置 projection，却以 total 命名。
- Fisheye `valid_mask_ms` 直接等于包含 image+mask 的 warp 时间，不是独立测量。
- Fisheye `map_cache.created/reused` 每帧固定为 true；`map_cache_build_ms` 每帧重复历史构造时间。
- B-2 metadata 写 `uses_equirectangular=False`，而 artifact 明确是 `equirectangular_rotation_only`。

## 9. QGC / FFmpeg 输出审计

真实链路已经存在，不再是 README 所称 placeholder：

```text
GUI current canvas 或 headless controller canvas
  -> FfmpegVideoSink (raw BGR stdin)
  -> libx264 ultrafast / zerolatency / no B frames
  -> UDP MPEG-TS 或 RTSP
  -> QGC standard video source
```

正面能力：

- GUI QGC 复用当前 stitched canvas，不启动第二套 camera/stitch pipeline。
- headless service 能独立 capture、stitch 和输出。
- FFmpeg 命令包含 wallclock timestamps、低延迟 preset/tune、短 GOP、UDP mux buffer 降低参数。
- 当前机器 FFmpeg/libx264 可用。

阻断问题：

- GUI 与 headless 都同步写 FFmpeg stdin；GUI 写点位于 Qt 主线程。
- `stderr=PIPE` 从未读取，warning/error 持续累积可填满 pipe。
- close 可能在 `stdin.close()`/flush 或 wait 上阻塞；kill 后没有再次 wait/reap。
- GUI Start 在 `canvas=None` 时先标 active，但不会真正启动 FFmpeg；错误后页面只显示 generic stopped，根因只进日志。
- QGC 运行中 widgets 仍可编辑；sink 使用启动时 snapshot，但状态文本每秒读取当前 widget，可能显示新 URL、实际仍推旧 URL。
- 切语言、导入 package、reload config 不会重建/停止 sink。
- headless 对完全相同的 frame 没有 signature/timestamp 去重，会按 process_fps 重算重编码。
- headless 异常只写 `_last_error` 并返回 false；主循环不打印、不退避、不退出、不重启，可长期“进程存活但无输出”。
- headless required cameras 只取 calibration topology，不扣除 camera config 中 disabled 相机；合法禁用一路可导致永久 Missing active camera frames。
- `--processor-mode candidate` 可与 `--mode near_field` 同时传入，但实际优先 B-2/Far status，CLI 缺少互斥 preflight。

现有 QGC 测试全部使用 FakeStreamManager/FakeController/NullVideoSink 或只断言 command list；尚未真实启动 FFmpeg/QGC，因此不能把单元测试通过当作输出验收。

## 10. 配置、候选与项目包迁移

### 10.1 配置持久化

优点：`save_yaml()` 使用同目录临时文件、fsync、`os.replace`，并支持内容 revision 冲突检查（`config.py:37-76`）。

缺点：

- revision check 与 replace 之间没有进程锁，两个进程仍可竞态。
- camera config 与 calibration config 分两次写，第二次失败会留下混合版本。
- auto backup 失败只记录日志，保存继续。
- backup 文件名只有秒级，同一秒内可能覆盖。
- Legacy `load_project()` 无 backup、confirmation、version/schema 校验、stale revision、事务或 rollback，GUI Open Project 直接执行。
- `cameras.yaml`/`network.yaml`、legacy project 和 package 会原样携带 RTSP/network credential；路径 scrub 不会删除 URI 凭据。

### 10.2 候选管理

- latest B-2 只要求 topology、rig complete 和 canvas 存在，不检查 `recommended`；因此当前 experimental/recommended=false candidate 被自动设为默认 Live strategy。
- `_candidate_ui_cache` 只按 path，不按 mtime/hash；同路径内容更新后 UI 仍可展示旧 metadata。
- 损坏的 newest `candidate.yaml` 可能抛 PyYAML ParserError；`latest_calibration_candidate()` 未捕获该异常，MainWindow 构造可直接失败。
- Far root 是 `PROJECT_ROOT/layout_candidates`，Near root 是 `PROJECT_ROOT/projects/layout_candidates`；前者未在 `.gitignore` 中。
- candidate/session/report 没有 retention。当前本地约 2.3 GiB，后续实验会持续膨胀。

### 10.3 项目包：什么能迁移，什么不能

正向实证：使用当前真实 latest B-2 + latest Near V2 在临时目录 export、复制整个 package directory 后：

- `validate(write_report=False)` 成功；
- moved `CandidatePanoramaProcessor` 能加载；
- moved Near runtime candidate 能加载；
- 临时 Far B-2 candidate 的 packaged provider 也能初始化。

因此，相对路径重写和“把整个目录移动后手工加载资源”基本成立。

但 activation 的真实行为是：

- manifest 写 `runtime_defaults` 和候选相对路径（`package.py:209-228`）；
- `activate_project_package()` 只复制 calibration/cameras/network（`package.py:343-378`）；
- 全仓没有 `runtime_defaults` 读取者；
- MainWindow 初始化 runtime config/candidates 为默认/空，import 后只 `reload_runtime_state()`。

所以导出时的 Far/Near mode、Far candidate、Near candidate、Fisheye source 不会恢复；重启后仍丢失。项目包复制到其他目录后是“资源可搬”，不是“项目状态可还原”。

### 10.4 validator / exporter 的严重缺陷

- required config 只检查文件存在；YAML parse error 在全包扫描时降为 warning，`valid` 仍可能为 true，activation 随后复制损坏 config。
- B-2 validation 只检查 candidate/remap 文件存在，不运行严格 loader、不 `np.load`、不验证 key/shape/dtype/format/resolution；现有测试 fixture 的 NPZ 甚至只是 `b"fake"`，format 与 resolution 也错误，但 test 仍通过。
- Far/Near validation 只检查 `candidate_type`，未复用已有严格 runtime loader。
- B-2 dependency collector 只拒绝绝对路径，不拒绝 `../`；destination 可逃出 package root，exporter 先写外部文件、之后才在 report 阶段报错，并留下 partial manifest。
- activation 按顺序 `copy2` 三个 config，没有 staging、整组原子替换或自动 rollback。
- `validate_project_package()` 默认写 `project_validation_report.yaml`，GUI Validate/Import/Activate 都会修改被检查包；只读介质和 checksum 交付不友好。
- Near candidate 声明 `masks/*.png`，通用 copier 不复制 masks；runtime 暂不依赖，但 provenance/human-review 引用损坏。

## 11. P0 / P1 / P2 / P3 问题清单

### 11.1 P0

**本轮未确认 P0。** 若项目包“一键恢复导出时全部运行态”被定义为当前硬交付承诺，则 P1-07 可按验收口径升为 P0；从代码安全与可恢复性看，当前仍建议标 P1，因为 active config 有备份路径，且尚未对外发布为正式包格式。

### 11.2 P1 — 发布/演示阻断

| ID | 问题 | 文件/函数证据 | 触发条件 | 影响 |
|---|---|---|---|---|
| P1-01 | Far Default Live 被 experimental B-2 自动截获，Template 控件隐藏；Live/Still 同名不同链 | `main_window.py:1671-1683,1820-1845,2575-2620,5082-5150`; `stitch_processing.py:84-102,166-185`; log `:4911-4915` | 存在 latest complete B-2；当前已满足 | 教师看到 Far-field，实际输出 unrecommended B-2；结果不可解释 |
| P1-02 | UI pending/applied/effective state 分裂 | candidate Load/Clear `main_window.py:1896-2064`; Apply `:2194-2289`; camera Save `:5876-5899`; reload `:5915-5956` | Live 时加载/清除候选、改源后保存、导入/reload | UI 显示新 mode/source，后台仍用旧 worker/capture/candidate |
| P1-03 | 断流旧帧继续拼接/QGC；全断仍显示 Live，无重连 | `stream_manager.py:121-155,354-368`; `main_window.py:5495-5535` | 一路失败、其他路继续；或全部 worker 退出 | 态势画面含无限陈旧区域，状态误导 |
| P1-04 | FFmpeg 同步 pipe 可冻结 GUI/headless，stderr 无 reader | `qgc/video_output.py:80-140`; `main_window.py:5668-5707,6125-6134`; `runtime_service.py:155-180` | encoder/network/backpressure、重复 warning、关闭 | UI 无响应、输出卡死、错误不可见 |
| P1-05 | non-daemon capture/stitch 线程关闭超时后仍可阻止进程退出 | `stream_manager.py:61,184-191,292-310`; `stitch_processing.py:46,133-141`; `main_window.py:5901-5905` | RTSP backend 不支持 timeout、USB read 卡住、重运算中关闭 | 窗口关了但 Python/FFmpeg/相机连接残留 |
| P1-06 | Layout Tuner target/projection cache 不失效，候选 provenance 可被污染 | `main_window.py:2815-2865,3391-3412,3454-3611,3665-3695` | Capture 后切 Near/Far/Current/Fisheye/参数，不重新 Capture 就 Save | Far 可基于 Current、Near 可基于 B-2，YAML 与实际基底不一致 |
| P1-07 | package activation 不读取 `runtime_defaults`，不能恢复运行态 | `package.py:209-228,343-378`; `main_window.py:1626-1629,5921-5946,6300-6303` | 包移动到另一目录/机器后 Activate 或重启 | 只能迁移 configs，不能复现 mode/candidate/Fisheye/画面 |
| P1-08 | package validation 对损坏 YAML、假 B-2/NPZ 存在假阳性 | `package.py:286-340,464-525`; `dependency_collector.py:32-55`; `calibration_candidate.py:1254-1265,1341-1350` | 包损坏、fixture/人工包只有占位文件 | validator 批准不可运行包，甚至将坏 config 激活 |
| P1-09 | candidate dependency 可用 `../` 造成 package-root 外写；activation/legacy restore 非事务 | `dependency_collector.py:43-55,96-106`; `package.py:235-243,361-371`; `project.py:38-46` | 恶意/损坏 candidate、copy 中途失败、打开 legacy project | 外部文件被覆盖、partial package、active configs 混合版本 |
| P1-10 | fresh checkout 不能直接启动/演示 | `.gitignore:2-3`; `main_window.py:1615-1618`; `StartDeepSharkViewStudio.cmd:4`; `requirements.txt` | 新团队机器、教师机器、无本地 ignored config/env | 缺 cameras.yaml 即启动失败；launcher 绑定个人 D 盘；依赖不可复现 |
| P1-11 | 30 FPS 配置无法兑现，缺 result/drop/age 指标；多个重任务仍在 GUI 线程 | `configs/cameras.yaml:41-46`; `main_window.py:3454-3623,6392-6422,7287-7309`; 微基准 | 当前三路 1080p，Far/Near/Fisheye/调参/求解 | 实际 3–16 FPS、首帧停顿、教师误以为 30 FPS、UI 卡顿 |
| P1-12 | 以像素非黑判断 valid mask 会改变真实内容 | `runtime_providers.py:48-68,386-393`; `layout_preview.py:90-150`; `topology.py:230-235` | 黑衣、暗场、阴影、真实黑色图案 | front 黑色目标可被侧路覆盖；Far feather 也把黑色当空洞 |
| P1-13 | live undistort 的 K/尺寸契约错误且只覆盖部分相机 | `stitcher.py:111-122`; `calibration.py:140-146`; `calibration.yaml:355-379,470-476` | 开启 Use undistort live；4K K 用于缩到 960 的帧，front 又无 formal K | Far Default/Near Current 几何失真且三路不一致 |
| P1-14 | Far Custom 契约不强：legacy source 可加载、blend 声明不生效、默认尺寸声明 700 实出 600、当前无工件 | `far_field_layout_candidate_runtime.py:79-115,177-207`; `far_field_custom_compositor.py:49-81,218-230`; `main_window.py:2881-2895` | 加载 legacy/矛盾 candidate 或使用默认 Far tuner | UI/manifest/runtime 不一致，无法准备稳定 Far Custom 演示 |
| P1-15 | `dual_horizontal` 当前配置从初始提交起即无效 | `calibration.yaml:8-74`; `topology.py:245-253`; `tests/test_topology.py:32-99` | 将 topology 切为 dual | seam x≈1354 位于 overlap 700–900 外，runtime 直接抛错；2-camera 支持声明不成立 |
| P1-16 | 当前正式几何与候选质量不足以作为 Demo RC 资产 | `calibration.yaml:85-102`; 最新 B-2 `:21-31,490-502` | 直接用当前配置/候选授课 | formal target 仍 initial template；B-2 report-only/unrecommended/非硬同步；无 Far candidate |
| P1-17 | OpenCV/OpenCL 基线出现一次 native crash，环境未锁定 | `test_calibration_candidate.py` 首次进程 `0xC0000005`；复跑 2/2 | 当前 OpenCV 4.13 + NVIDIA OpenCL cache | 不是 Python 可捕获错误；演示可能直接退出，需独立 GPU 验收 |

### 11.3 P2 — 重要但可在明确限制下绕过

| ID | 问题 | 文件/触发/影响 |
|---|---|---|
| P2-01 | Near V3 projection 优先级不对称 | `stitch_runtime_controller.py:65-77,247-269`；V3 Fisheye candidate + UI 选 Current 后，worker 静默改回 Fisheye，UI 仍显示 Current；Near notice 也固定写 Current（`main_window.py:5065-5069`）。 |
| P2-02 | candidate schema 与 runtime 约束不一致 | Far blend/use_profile_overlaps 未真正使用；vertical safety enabled、projection_pipeline 语义弱；loader scale 0.5–1.5，renderer 静默 clip 0.8–1.2。 |
| P2-03 | B-2 weight 对 `-inf` 做线性 affine 可产生 NaN | `calibration_candidate.py:1364-1368`、`far_field_custom_compositor.py:193-207`；非 identity scale/小数中心时边缘可能出现 hole/selection 不稳定。 |
| P2-04 | headless 重复处理相同帧、disabled 语义与 GUI 不同、异常静默循环 | `qgc/runtime_service.py:144-180,218-249,304-348`；浪费计算，进程可“活着但无输出”。 |
| P2-05 | QGC UI runtime snapshot 与 widgets 分离 | Start 无 canvas、运行中改 URL、切语言、Import/Reload 后 status 可与真实 sink 不同；根因只写日志。 |
| P2-06 | validate 默认修改包；Near masks 不复制；包会携带凭据 | `package.py:258-339`; `dependency_collector.py:65-84`; `project.py:16-27`；影响只读介质、checksum、provenance 与安全分发。 |
| P2-07 | backup/多文件保存可靠性不足 | `project.py:49-53`; `main_window.py:5828-5892`；秒级覆盖、backup 失败仍写、第二文件失败留下部分更新。 |
| P2-08 | corrupt newest candidate 可阻断 GUI 启动，UI metadata cache 不看 revision | `calibration_candidate.py:1269-1305`; `main_window.py:1671-1683,4988-5003`。 |
| P2-09 | Far/Near candidate root 不一致 | Far `PROJECT_ROOT/layout_candidates`，Near `PROJECT_ROOT/projects/layout_candidates`；Far 会污染未忽略的仓库顶层。 |
| P2-10 | 无 artifact retention | 当前约 2.3 GiB；session、seam、projection 工件持续增长，长期运行可能占满磁盘。 |
| P2-11 | hidden Stitched tab 仍渲染；错误可逐帧刷日志 | `main_window.py:5626-5707,8298-8314`；浪费 UI 主线程，日志控件内存无界增长。 |
| P2-12 | B-2 OpenCL 不返回 warped previews | `calibration_candidate.py:1486-1545`；canvas/QGC 可有图，Warped Views 全空，诊断体验不一致。 |
| P2-13 | Current/Fisheye/Far 的缓存和 identity fast path 不足 | `front_priority_compositor_core.py:41-56`; `camera_layout_adjust.py:50-100`; `runtime_providers.py:319-383`；重复 warp 静态 mask、重复 affine、跨 controller 不复用。 |
| P2-14 | UI 信息架构与本地化未闭环 | Projection Research 空页、运行控件分散、English 模式仍有大量中文直写、Project/Calibration 对话文字不统一。 |
| P2-15 | README/quick start 与实际实现不一致 | README 仍描述三 workspace、streaming placeholder 和未来 QGC；实际已有六 workspace/QGC/Far/Near，团队容易按错流程。 |
| P2-16 | 实际相机源与 health 表展示源可不同 | 保存 source/enabled 不重启 capture；health 取 widget source，而 worker 仍拉旧 URL。 |

### 11.4 P3 — 清理与长期维护

| ID | 问题 | 影响 |
|---|---|---|
| P3-01 | `main_window.py` 8,324 行单类 | 修改局部工作流容易破坏无关状态；测试需大量 patch。 |
| P3-02 | Legacy Project 与 Portable Project Package 两套相似入口并排 | `.dsvs.yaml` 与 `project.dcsvs.yaml`、安全语义和用户预期容易混淆。 |
| P3-03 | `resolve_project_path()` 强制 `/`→`\` | package 格式实质 Windows-only；“其他机器”不等于跨平台。 |
| P3-04 | `copy_b2_candidate_directory()` 重复复制 candidate/report | 小幅重复 I/O 和代码噪音。 |
| P3-05 | 测试名/文档有过时文字 | 如 `test_far_field_layout_tuner_note_says_not_b2` 实际断言包含 B-2；降低维护可读性。 |
| P3-06 | `streaming.py` 仍是旧 placeholder | 与真实 `qgc/video_output.py` 并存，架构入口容易误判。 |
| P3-07 | 无统一 schema 定义/版本迁移层 | configs、candidate、manifest 都靠分散手写 dict/parser，演进成本高。 |

## 12. 测试体系审计

### 12.1 本轮结果

| 范围 | 结果 |
|---|---:|
| 全部识别到的 unittest 方法 | 194 |
| 通过 | 191 |
| 失败 | 3 |
| Python 断言之外的 native crash | 1 次，复跑通过 |

失败分类：

1. `tests/test_topology.py::test_dual_profile_geometry_is_horizontal_and_isolated`：**实际问题**。当前 dual seam 平均 x≈1353.99，不在 `[700,900]` overlap。
2. `tests/test_topology.py::test_dual_output_is_left_right_not_top_bottom`：**实际问题**。`compose_horizontal_feather()` 运行时按设计拒绝 seam-outside-overlap。
3. `tests/test_preview_state.py::test_calibration_page_displays_triple_topology_diagnostics`：**过时断言**。配置在提交 `a477676` 已把 triple feather 从 160 改为 400，UI 正确显示当前 400，测试仍期待 160。

额外稳定性：`test_calibration_candidate.py` 首次进程退出 `-1073741819 / 0xC0000005`，faulthandler 复跑 2/2；OpenCV 同时处理旧 NVIDIA OpenCL cache。结论是环境/native 风险，不是旧断言。

### 12.2 现有测试的优点

- 纯 unittest，无 pytest 依赖也可运行。
- config 原子写、revision conflict、candidate 不写 formal calibration、session gate、projection provider、Near/Far controller、seam/front-priority、package relative path 都有覆盖。
- 多数生成型测试使用临时目录，并检查 formal calibration hash 不变。
- GUI offscreen 测试覆盖六 workspace、基本状态、wizard 和 fake QGC sink。

### 12.3 关键覆盖缺口

- GUI Live 的 effective mode/provider/composition 矩阵，以及 Live/Still 一致性。
- pending selection 与 applied worker 的原子状态和回执。
- stale frame eviction、frame age、all-failed、reconnect、真实 backend timeout。
- target/projection cache invalidation与 candidate provenance。
- 黑色有效像素、Far output size、source/blend cross-validation、V3 projection precedence。
- OpenCL warped output 与 native fallback。
- 真实 FFmpeg process、stderr/backpressure、QGC UDP/RTSP、close/reap。
- package corrupt YAML、fake/truncated NPZ、`../` dependency、activation rollback、runtime_defaults restore、重启恢复。
- fresh checkout bootstrap 和 hardcoded launcher。

特别注意：`test_project_package.py` 的 moved-package 用例只调用 validator；fixture 使用错误 B-2 format、1280×720 和 `b"fake"` NPZ，却仍通过，当前测试实际上固化了 validator 假阳性。

## 13. 需要实机验证或正式 profiling 的事项

| 项目 | 必须记录 | 通过标准建议 |
|---|---|---|
| 模式真值矩阵 | Still/Live/headless × Far Default、B-2 View、Far Custom、Near Current、Near Fisheye 的 UI label、`runtime_status`、provider、composition、size | 每一项完全一致；Far Default 不再被隐式 candidate 覆盖 |
| 三路时序 | 每路 capture timestamp、frame age、pair skew、硬件/软件同步说明 | 超阈值明确 Degraded；不得静默拼旧帧 |
| 断流/恢复 | 单路断、全断、网络恢复、设备拔插、认证失败 | 无 stale frame；状态进入 Degraded/Failed；可控重连 |
| 关闭生命周期 | RTSP read 阻塞、Near/B-2 正在处理、FFmpeg active 时关闭 | 2–5 秒内 Python/FFmpeg/CameraStream 全部退出 |
| FFmpeg/QGC | UDP 与 RTSP(MediaMTX)各 30 分钟；ffprobe/QGC 记录 FPS、PTS、端到端延迟、丢包 | 无 pipe 堵塞、无 PTS 回退、UI 可操作、错误可恢复 |
| 资源 | 各模式 QGC on/off 的 CPU/GPU/RSS、内存带宽、p50/p95 stitch、UI event latency、submitted/dropped/result FPS | 形成模式级预算，默认 FPS 不高于可兑现 p95 |
| B-2 OpenCL | CPU/OpenCL 画面一致性、driver/cache cold start、fallback、Warped Views | 不崩溃；fallback 可见；diagnostics 一致 |
| Far Custom | identity/offset/scale，检查 `-inf` affine 的 NaN/hole、实际 output size | 无 hole；candidate 声明尺寸等于 runtime 尺寸 |
| Near Current 暗场 | 黑衣、黑板、阴影、纯黑目标跨 seam | 黑色内容仍被视作有效几何 |
| Near Fisheye | 1920×1080 三路真实 fisheye、不同 balance/FOV、template warp 后 seam | 重新调出的 candidate 有可重复画质；不沿用未经验证的 Current 参数 |
| 项目包 | export→移动→validate→activate→重启→处理三帧 | mode/candidate/provider/size/画面均复现，原包 checksum 不变 |
| 教师机 | 干净 Windows 用户，无 D 盘开发 env | 按文档一次启动，自动/明确生成本地 config，preflight 全绿 |

## 14. 分阶段最小修复路线图

### 阶段 A：先建立“运行真值”和安全停止线

1. 定义唯一 `EffectiveRuntimeState`：明确 `far_default`、`b2_candidate_view`、`far_custom_b2`、`near_current`、`near_fisheye`；B-2 View 不再自动截获 Far Default。
2. UI 选择作为 pending；Apply 后由 worker 回执 applied state。summary、overlay、QGC status 全部以 result 的 effective status/provider 为真源。
3. Load/Clear、camera Save、Import/Reload/语言切换均走统一原子 transition；必要时 stop/restart capture、stitch、QGC。
4. 加 frame age/status 驱逐；单路 failure 不再复用旧帧；all-failed 进入明确状态。
5. close 必须检查 worker/sink shutdown 结果；实现可取消/可诊断的退出，不允许 non-daemon 残留。

验收出口：模式矩阵、断流、保存源、切语言、导入、关闭的 GUI 集成测试全部通过。

### 阶段 B：FFmpeg/QGC 与 headless 可靠性

1. FFmpeg 使用独立 bounded latest-frame writer；GUI 线程只提交引用/拷贝，不能阻塞 pipe。
2. 持续 drain stderr，保存最近错误、exit code、restart count；实现 health watcher 和幂等 close/reap。
3. headless 加 frame signature/age、startup preflight、错误日志/退避/退出策略；统一 enabled camera 语义。
4. QGC widgets 在 active 时锁定或明确“Apply/Restart Output”；status 显示启动时 effective config。

验收出口：UDP/RTSP 30 分钟、kill FFmpeg、暂停 receiver、断网、关闭均通过。

### 阶段 C：候选正确性与性能

1. tuner cache key 包含 target、projection、intrinsics hash、balance/FOV、frame signature；任一变化立即标 stale 并禁止 Save。
2. 明确 V3 projection 权威来源；candidate 与 UI override 不允许不对称静默覆盖。
3. 用几何全 1 mask 经过同一 warp 推导 Current valid mask，禁止从像素颜色推断。
4. 修复 K 的分辨率缩放或 undistort/resize 顺序，并要求三路 intrinsics 完整一致。
5. 强校验 Far source/blend/output canvas；修复 1800×700→1800×600 静默夹断、`-inf` affine NaN、candidate root。
6. 增加 identity fast path、Near precomputed plan、Fisheye mask/cache、Far Custom 轻量 B-2 provider；后台化 tuner/solver/diagnostics。
7. 以实测 result p95 反推各模式默认 process FPS，并展示 drop/result/frame-age。

验收出口：暗场、Far scale、Fisheye、OpenCL 一致性和模式级资源预算均有记录。

### 阶段 D：package/persistence 安全门

1. required config YAML 解析失败直接 invalid；做最低 schema/type/profile 校验。
2. Far/Near validation 复用正式 runtime loader；B-2 validation 严格 load candidate/report/NPZ key/shape/dtype。
3. 所有 dependency 在复制前 resolve 并验证 containment；拒绝任何 `..` escape。
4. export 先写 staging，完成并自验证后原子 rename；失败清理 partial package。
5. activation 先 staging 三份 config，整组验证后替换；中途失败自动 rollback。
6. 实现 `runtime_defaults` 读取、包内 candidate/source 解析与 active-package context 持久化；重启后恢复。
7. Legacy Open Project 至少先确认、备份、schema 校验并事务化；长期建议迁移到单一 portable package 入口。
8. Validate 默认只读；显式选择后才写报告。导出前提供 credential scrub/清单。

验收出口：corrupt YAML、fake NPZ、escape、copy failure、move+restart 全部自动化测试通过。

### 阶段 E：Team Beta / Demo RC 准备

1. first-run 从 example 安全初始化缺失 local config，不覆盖已有文件；launcher 不硬编码个人 env。
2. 增加版本锁、Python/OpenCV/PySide/FFmpeg preflight、标准 test/lint/CI 命令。
3. 修复 dual profile 或明确删除其支持声明；同步 README/quick start/QGC/package 文档。
4. 拆分 MainWindow orchestration：RuntimeStateCoordinator、CaptureController、ProjectService、QgcOutputController、Calibration/Layouts view-model。
5. 增加 artifact retention/配额/清理策略。
6. 制作去密 demo package、固定三路输入、正式 Far Default、已验证 Far Custom candidate、Near Current、Near Fisheye，并保存验收记录。

Team Beta 门：干净 checkout 可启动、CI 全绿、package move+restart 可复现、无 P1 生命周期问题。  
Demo RC 门：再加实机三路/QGC/断流/关闭/性能/画质证据与完整教师 smoke 记录。

## 15. 建议验收命令

以下命令建议在修复分支的 disposable copy 或专用验收环境执行；GUI 测试会生成/轮转应用日志，正式只读校验应继续使用内存 logger 或临时项目根。

### 15.1 CPU 稳定基线

```powershell
$env:PYTHONDONTWRITEBYTECODE = "1"
$env:QT_QPA_PLATFORM = "offscreen"
$env:OPENCV_OPENCL_RUNTIME = "disabled"
$py = "D:\Develop\envs\deep-shark-view-studio\Scripts\python.exe"

& $py -B -m unittest discover -s tests -p "test_*.py" -v
& $py -B -m unittest discover -s tests\seam -p "test_*.py" -v
git status --short
```

### 15.2 OpenCL 独立基线

```powershell
$env:PYTHONDONTWRITEBYTECODE = "1"
Remove-Item Env:OPENCV_OPENCL_RUNTIME -ErrorAction SilentlyContinue
& $py -B -X faulthandler -m unittest discover -s tests -p "test_calibration_candidate.py" -v
```

OpenCL 不应混入 CPU 必过门；它应作为单独、可降级的能力验收。

### 15.3 所有 topology invariant

```powershell
& $py -B -c "from deep_shark_studio.config import load_config; from deep_shark_studio.topology import validate_overlap_seams; c=load_config('calibration.yaml'); bad={n:validate_overlap_seams(p) for n,p in c.get('topologies',{}).items() if isinstance(p,dict) and not p.get('legacy_root') and validate_overlap_seams(p)}; assert not bad,bad"
```

当前该命令应因 `dual_horizontal` 失败；修复后必须通过。

### 15.4 QGC/FFmpeg preflight

```powershell
ffmpeg -version
& $py -B -m deep_shark_studio.qgc.runtime_service --help
```

随后必须运行真实 UDP/RTSP smoke，而不能只看 `--help` 或 command-list 单测。

### 15.5 必须新增后再执行的自动验收

建议新增独立测试模块并固定命令：

```powershell
& $py -B -m unittest -v `
  tests.test_effective_runtime_state `
  tests.test_live_failure_recovery `
  tests.test_ffmpeg_sink_lifecycle `
  tests.test_project_package_roundtrip `
  tests.test_clean_checkout_bootstrap
```

其中 package roundtrip 必须覆盖：export → copytree → validate(read-only) → strict-load B-2/Far/Near/Fisheye → activate temp configs → restore runtime defaults → process frames → close/reopen → 再次验证；并包含 corrupt YAML、truncated NPZ、`../` dependency 和 copy failure rollback。

## 16. 教师演示 smoke checklist

### 安装与材料

- [ ] 在非开发者 Windows 用户/目录启动，不依赖 `D:\Develop\envs`。
- [ ] 缺失 local config 时有安全 bootstrap；不会覆盖教师机器已有配置。
- [ ] Python/OpenCV/PySide/FFmpeg 版本与验收记录一致。
- [ ] demo package 已去除 RTSP/network credential。
- [ ] package Validate 不修改原包；checksum 前后一致。
- [ ] 有固定三路输入或实机，且分辨率/相机顺序明确。
- [ ] 有已验证的 Far-field Layout Candidate；当前仓库尚无。

### 模式真值

- [ ] 启动默认明确显示并实际运行 Far-field Default/template。
- [ ] B-2 Candidate View 是显式 experimental 选项，不自动覆盖 Default。
- [ ] Far Custom 页面显示 B-2 provider、candidate path、`b2_weight_selection`、实际 1800×600 或经验证的目标尺寸。
- [ ] Near Current 显示 Current provider、Layout Candidate path。
- [ ] Near Fisheye 显示 Fisheye provider、intrinsics path、balance/FOV，并使用重新调过的 layout。
- [ ] Still、Live、headless/QGC 对同一模式给出相同 effective status/provider/composition。

### 运行与故障

- [ ] 三路 Live 状态、frame age、result FPS、drop ratio 可见。
- [ ] 单路断流后该路旧帧不再进入拼接；页面显示 Degraded。
- [ ] 全路断流后停止刷新并显示 Failed/Stopped，不再声称 Live。
- [ ] 网络恢复后按设计重连或给出明确手工恢复步骤。
- [ ] 切 candidate、改 source/enabled、导入项目、切语言后 UI 与后台一致。
- [ ] 相同持续错误不会 30 次/秒刷日志。
- [ ] Start/Stop Live 与 Start/Stop QGC 可重复三次，无残留 worker/process。

### QGC

- [ ] UDP MPEG-TS 在 QGC 中连续播放，实际 FPS/PTS/延迟有记录。
- [ ] RTSP + MediaMTX 在 QGC 中连续播放，断网/恢复有记录。
- [ ] kill FFmpeg、暂停 receiver、错误 URL 时 UI 不冻结并显示根因。
- [ ] QGC active 时编辑 URL 要么被锁定，要么明确 Apply/Restart 后才生效。

### 项目迁移与收尾

- [ ] 导出 package，复制到全新目录后 Validate/Activate。
- [ ] 激活后显示 package 名、mode、Far/Near/Fisheye candidate 相对路径。
- [ ] 重启应用后 effective state 不变。
- [ ] activation 失败能自动回滚到完整旧配置。
- [ ] 演示结束能从明确的 backup 恢复。
- [ ] 关闭应用 2–5 秒后，Python、FFmpeg 和 CameraStream 线程/进程全部消失。

## 17. 最终成熟度判定

### Developer Build：当前符合

- 四条研究/运行链已形成，provider/compositor/candidate 分层基本可辨。
- 有 194 个 unittest 方法和较好的只读候选边界意识。
- 有实际 GUI、QGC sink、项目包和本地实验工件。
- 但仍依赖开发者机器的 ignored config、硬编码环境、人工候选选择和开发者解释。

### Team Beta：当前不符合

缺少 fresh-checkout 启动、版本锁/CI、确定性 effective mode、断流/关闭韧性、严格 package roundtrip、事务恢复和可兑现性能预算。

### Demo Release Candidate：当前明确不符合

当前默认 Live 会隐式使用 experimental/recommended=false B-2；formal geometry 自述不是最终标定；没有 Far Custom 成品 candidate；Near/Fisheye 未完成实机闭环；QGC/FFmpeg 未端到端验证；当前 30 FPS 设置与本机实际能力相差显著；三项测试失败且 OpenCL 出现过 native crash。

**结论：保持 Developer Build。完成阶段 A–D 并达到 Team Beta 门后，再准备固定工件和实机/QGC 证据进入 Demo RC 评审。**

---

本报告完成即停止。本轮未修改任何代码、配置、候选文件或项目数据；未进入修复阶段。
